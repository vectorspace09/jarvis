export class NotificationManager {
  scheduleNotification(notification: {
    type: 'reminder' | 'suggestion' | 'alert';
    message: string;
    triggerTime: Date;
  }) {
    // Implementation
  }
} 